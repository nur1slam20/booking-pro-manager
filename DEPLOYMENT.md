# 🚀 Инструкция по деплою Booking Manager PRO

## 📋 Обзор

Этот проект можно задеплоить на различные платформы. Мы рекомендуем **Render.com** или **Railway.app** для backend и **Vercel** или **Netlify** для frontend.

---

## 🔧 Шаг 1: Деплой Backend (Render.com)

### 1.1 Подготовка репозитория

1. Убедитесь, что все файлы закоммичены в Git:
```bash
git add .
git commit -m "Prepare for deployment"
git push origin main
```

### 1.2 Создание аккаунта на Render

1. Перейдите на [render.com](https://render.com)
2. Зарегистрируйтесь или войдите
3. Подключите свой GitHub аккаунт

### 1.3 Создание PostgreSQL базы данных

1. В Dashboard нажмите **"New +"** → **"PostgreSQL"**
2. Заполните форму:
   - **Name**: `booking-pro-db`
   - **Database**: `booking_db`
   - **User**: `booking_user`
   - **Region**: выберите ближайший (например, `Frankfurt`)
   - **PostgreSQL Version**: `16` (или последняя доступная)
   - **Plan**: Free (для начала)
3. Нажмите **"Create Database"**
4. **ВАЖНО**: Скопируйте **Internal Database URL** (он понадобится позже)

### 1.4 Создание Web Service

1. В Dashboard нажмите **"New +"** → **"Web Service"**
2. Подключите репозиторий с вашим проектом
3. Заполните настройки:

   **Basic Settings:**
   - **Name**: `booking-pro-api`
   - **Environment**: `Node`
   - **Region**: тот же, что и у базы данных
   - **Branch**: `main`
   - **Root Directory**: `.` (оставьте пустым)

   **Build & Deploy:**
   - **Build Command**: `npm install`
   - **Start Command**: `npm start`

   **Environment Variables:**
   ```
   NODE_ENV=production
   PORT=10000
   DATABASE_URL=<Internal Database URL из шага 1.3>
   JWT_SECRET=<сгенерируйте случайную строку, например через: openssl rand -hex 32>
   JWT_EXPIRES_IN=7d
   FRONTEND_URL=<URL вашего frontend, например: https://your-frontend.vercel.app>
   ```

4. Нажмите **"Create Web Service"**

### 1.5 Запуск миграций

После первого деплоя нужно выполнить миграции:

1. В Dashboard вашего Web Service перейдите во вкладку **"Shell"**
2. Выполните команду:
```bash
psql $DATABASE_URL -f migrations/001_create_tables.sql
```

Или создайте временный скрипт для миграций:

```bash
# В Shell на Render
cat migrations/001_create_tables.sql | psql $DATABASE_URL
```

### 1.6 Создание тестового админа

После миграций создайте тестового админа:

```bash
# В Shell на Render
node scripts/create-admin.js
```

Или через SQL:
```sql
INSERT INTO users (name, email, password, role)
VALUES (
  'Admin',
  'admin@example.com',
  '$2b$10$YourBcryptHashHere',
  'admin'
);
```

**Примечание**: Пароль должен быть захеширован через bcrypt. Используйте скрипт `scripts/create-admin.js` или временно добавьте его в проект.

---

## 🌐 Шаг 2: Деплой Frontend (Vercel)

### 2.1 Подготовка

1. Убедитесь, что frontend код в репозитории (папка `frontend/`)
2. В папке `frontend/` создайте файл `.env.production`:
```env
VITE_API_URL=https://your-backend-url.onrender.com
```

### 2.2 Создание проекта на Vercel

1. Перейдите на [vercel.com](https://vercel.com)
2. Зарегистрируйтесь или войдите
3. Нажмите **"New Project"**
4. Подключите ваш GitHub репозиторий
5. Настройте проект:

   **Framework Preset**: `Vite`
   **Root Directory**: `frontend`
   **Build Command**: `npm run build`
   **Output Directory**: `dist`

   **Environment Variables:**
   ```
   VITE_API_URL=https://your-backend-url.onrender.com
   ```

6. Нажмите **"Deploy"**

### 2.3 Обновление CORS на Backend

После деплоя frontend, обновите переменную `FRONTEND_URL` в настройках вашего Render Web Service на реальный URL frontend.

---

## 🔄 Альтернатива: Railway.app (Backend)

### Создание проекта на Railway

1. Перейдите на [railway.app](https://railway.app)
2. Зарегистрируйтесь через GitHub
3. Нажмите **"New Project"** → **"Deploy from GitHub repo"**
4. Выберите ваш репозиторий
5. Railway автоматически определит Node.js проект
6. Добавьте PostgreSQL:
   - Нажмите **"+ New"** → **"Database"** → **"Add PostgreSQL"**
7. Настройте переменные окружения:
   - `DATABASE_URL` будет автоматически добавлена
   - Добавьте остальные переменные как на Render
8. Railway автоматически запустит деплой

---

## ✅ Проверка деплоя

### Backend

1. Проверьте health check:
```bash
curl https://your-backend-url.onrender.com/health
```

Должен вернуться:
```json
{"message":"API работает","timestamp":"..."}
```

2. Проверьте API info:
```bash
curl https://your-backend-url.onrender.com/
```

### Frontend

1. Откройте ваш frontend URL в браузере
2. Попробуйте зарегистрироваться
3. Попробуйте войти
4. Проверьте создание бронирования

---

## 📝 Тестовые данные для проверки

### Создание тестового пользователя

После деплоя зарегистрируйтесь через frontend или используйте API:

```bash
curl -X POST https://your-backend-url.onrender.com/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Test User",
    "email": "test@example.com",
    "password": "test123456"
  }'
```

### Создание тестового админа

Для создания админа используйте скрипт или SQL (см. шаг 1.6).

---

## 🔒 Безопасность

### Важные моменты:

1. **JWT_SECRET**: Должен быть случайной строкой (минимум 32 символа)
   ```bash
   openssl rand -hex 32
   ```

2. **DATABASE_URL**: Никогда не коммитьте в Git, используйте переменные окружения

3. **CORS**: Убедитесь, что `FRONTEND_URL` указан правильно

4. **Rate Limiting**: Уже настроен в коде (200 запросов за 15 минут)

---

## 🐛 Решение проблем

### Backend не запускается

1. Проверьте логи в Render Dashboard
2. Убедитесь, что все переменные окружения установлены
3. Проверьте, что база данных доступна (проверьте Internal Database URL)

### Миграции не работают

1. Убедитесь, что вы используете правильный DATABASE_URL
2. Проверьте права доступа к базе данных
3. Попробуйте выполнить миграции вручную через Shell

### Frontend не подключается к Backend

1. Проверьте `VITE_API_URL` в переменных окружения Vercel
2. Проверьте CORS настройки на backend (переменная `FRONTEND_URL`)
3. Проверьте, что backend доступен публично

---

## 📚 Дополнительные ресурсы

- [Render Documentation](https://render.com/docs)
- [Vercel Documentation](https://vercel.com/docs)
- [Railway Documentation](https://docs.railway.app)
- [PostgreSQL на Render](https://render.com/docs/databases)

---

## ✅ Чек-лист деплоя

- [ ] Backend задеплоен на Render/Railway
- [ ] База данных PostgreSQL создана
- [ ] Миграции выполнены
- [ ] Тестовый админ создан
- [ ] Frontend задеплоен на Vercel/Netlify
- [ ] CORS настроен правильно
- [ ] Все переменные окружения установлены
- [ ] Health check работает
- [ ] API тестируется через frontend
- [ ] README.md обновлен с production URLs

---

**Удачного деплоя! 🚀**

