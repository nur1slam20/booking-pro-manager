# 🚀 Пошаговая инструкция деплоя на Render

## ✅ Ваш сгенерированный JWT_SECRET (сохраните его!):
```
22ddcd443115bb6c5dbe19a73e92470acea3d85b9a4f4ae37335f72a8cef4fcf
```

---

## 📋 ШАГ 1: Создать PostgreSQL базу данных

1. Откройте [render.com](https://render.com) и **войдите** (или зарегистрируйтесь через GitHub)

2. В Dashboard нажмите кнопку **"New +"** (вверху справа)

3. Выберите **"PostgreSQL"**

4. Заполните форму:
   - **Name**: `booking-pro-db`
   - **Database**: `booking_db`
   - **User**: `booking_user`
   - **Region**: выберите `Frankfurt` (или ближайший к вам)
   - **PostgreSQL Version**: `16` (или последняя доступная)
   - **Plan**: `Free` (для начала)
   
5. Нажмите **"Create Database"**

6. **ВАЖНО**: Дождитесь создания базы (30-60 секунд), затем:
   - Найдите раздел **"Connections"**
   - Скопируйте **"Internal Database URL"** (он начинается с `postgresql://...`)
   - **Сохраните этот URL**, он понадобится в следующем шаге!

---

## 🌐 ШАГ 2: Создать Web Service (Backend API)

1. В Dashboard снова нажмите **"New +"** → **"Web Service"**

2. В разделе **"Connect a repository"**:
   - Если ваш GitHub аккаунт не подключен, нажмите **"Configure account"** и подключите
   - Найдите и выберите репозиторий: `nur1slam20/booking-pro-manager`
   - Нажмите **"Connect"**

3. Заполните настройки:

### Basic Settings:
   - **Name**: `booking-pro-api`
   - **Environment**: `Node`
   - **Region**: тот же, что и у базы (`Frankfurt`)
   - **Branch**: `main`
   - **Root Directory**: оставьте **пустым** (точка `.` по умолчанию)

### Build & Deploy:
   - **Build Command**: `npm install`
   - **Start Command**: `npm start`

### Environment Variables:
   Нажмите **"Advanced"** → **"Add Environment Variable"** и добавьте каждую переменную:

   | Key | Value |
   |-----|-------|
   | `NODE_ENV` | `production` |
   | `PORT` | `10000` |
   | `DATABASE_URL` | `<вставьте Internal Database URL из шага 1>` |
   | `JWT_SECRET` | `22ddcd443115bb6c5dbe19a73e92470acea3d85b9a4f4ae37335f72a8cef4fcf` |
   | `JWT_EXPIRES_IN` | `7d` |
   | `FRONTEND_URL` | `http://localhost:5173` (пока, потом обновим) |

4. Нажмите **"Create Web Service"**

5. Render начнёт деплой (это займёт 2-3 минуты)

---

## 🔍 ШАГ 3: Проверить деплой

1. Дождитесь, пока деплой завершится (статус станет "Live")

2. В настройках сервиса найдите **"Service URL"** (что-то вроде `https://booking-pro-api.onrender.com`)

3. Проверьте health check:
   - Откройте в браузере: `https://your-service-url.onrender.com/health`
   - Должно вернуться: `{"message":"API работает","timestamp":"..."}`

4. Проверьте API info:
   - Откройте: `https://your-service-url.onrender.com/`
   - Должна вернуться информация об API

---

## 🗄️ ШАГ 4: Выполнить миграции базы данных

1. В Dashboard вашего Web Service откройте вкладку **"Shell"** (вверху)

2. Выполните команду:
```bash
psql $DATABASE_URL -f migrations/001_create_tables.sql
```

3. Должно появиться сообщение о создании таблиц (без ошибок)

---

## 👤 ШАГ 5: Создать тестового администратора

В том же Shell выполните:

```bash
ADMIN_EMAIL=admin@example.com ADMIN_PASSWORD=admin123456 npm run create-admin
```

Должно появиться:
```
✅ Администратор успешно создан!
📋 Данные для входа:
   Email: admin@example.com
   Password: admin123456
   Role: admin
```

**Сохраните эти данные!**

---

## ✅ Проверка работоспособности

### Тест 1: Health Check
```bash
curl https://your-service-url.onrender.com/health
```

### Тест 2: Регистрация пользователя
```bash
curl -X POST https://your-service-url.onrender.com/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Test User",
    "email": "test@example.com",
    "password": "test123456"
  }'
```

### Тест 3: Вход админа
```bash
curl -X POST https://your-service-url.onrender.com/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "email": "admin@example.com",
    "password": "admin123456"
  }'
```

---

## 📝 Что дальше?

После успешного деплоя backend:

1. ✅ Обновите `FRONTEND_URL` в настройках Render на реальный URL frontend (после деплоя frontend)
2. ✅ Задеплойте frontend на Vercel (следующий шаг)
3. ✅ Обновите README.md с production URLs

---

## 🐛 Решение проблем

### Backend не запускается:
- Проверьте логи в разделе "Logs" вашего сервиса
- Убедитесь, что все переменные окружения установлены
- Проверьте, что DATABASE_URL правильный

### Миграции не работают:
- Убедитесь, что вы используете Shell в Web Service (не в базе данных)
- Проверьте, что файл миграций существует: `ls migrations/`

### Health check возвращает ошибку:
- Проверьте логи: "Logs" → найдите ошибки
- Убедитесь, что порт установлен как `10000` (Render использует этот порт)

---

**Готовы начать? Следуйте шагам по порядку! 🚀**

postgresql://booking_user:suAISv5mCYyqsibVP6oMMSTRlnMG6x6F@dpg-d50oj6ffte5s73cs393g-a/booking_db_tpwg