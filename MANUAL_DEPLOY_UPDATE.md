# 🔄 Принудительное обновление деплоев

## ⚠️ Если автоматическое обновление не сработало:

---

## 📱 Vercel (Frontend) - Ручное обновление:

### Способ 1: Через Dashboard (РЕКОМЕНДУЕТСЯ)

1. Откройте [vercel.com](https://vercel.com) и войдите
2. Найдите ваш проект `booking-pro-manager`
3. На главной странице проекта найдите секцию **"Deployments"**
4. Найдите последний deployment (должен быть с коммитом `ce97ee7`)
5. Если его нет или он старый:
   - Нажмите кнопку **"..."** (три точки) рядом с последним deployment
   - Или найдите кнопку **"Redeploy"** вверху
   - Выберите **"Redeploy"** → **"Use existing Build Cache"** (или без кэша для полной пересборки)
6. Подождите 1-2 минуты

### Способ 2: Через Settings

1. Откройте проект в Vercel Dashboard
2. Перейдите в **"Settings"** → **"Git"**
3. Убедитесь, что подключен правильный репозиторий: `nur1slam20/booking-pro-manager`
4. Вернитесь на главную страницу проекта
5. Нажмите **"Redeploy"**

---

## 🔧 Render (Backend) - Ручное обновление:

1. Откройте [dashboard.render.com](https://dashboard.render.com) и войдите
2. Найдите ваш Web Service `booking-pro-api`
3. В настройках сервиса найдите вкладку **"Manual Deploy"** (слева в меню)
4. Нажмите **"Deploy latest commit"**
5. Или выберите конкретный коммит из списка (выберите `ce97ee7` - последний)
6. Подождите 2-3 минуты

---

## ✅ Проверка после обновления:

### Frontend:
1. Откройте ваш Vercel URL в браузере
2. Проверьте админ-панель → должны появиться новые разделы (Services, Bookings, Users)

### Backend:
1. Проверьте health check: `curl https://your-backend-url.onrender.com/health`
2. Должен вернуться: `{"message":"API работает"...}`

---

## 🐛 Если всё равно не работает:

### Проверьте подключение к GitHub:

**Vercel:**
1. Settings → Git
2. Убедитесь, что репозиторий подключен: `nur1slam20/booking-pro-manager`
3. Branch должен быть: `main`

**Render:**
1. Settings → Build & Deploy
2. Убедитесь, что Repository подключен: `nur1slam20/booking-pro-manager`
3. Branch должен быть: `main`

---

**После ручного обновления все изменения будут применены!** ✅


