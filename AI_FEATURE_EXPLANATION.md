# 🤖 Как работает AI-фича рекомендаций

## 📋 Общее описание

AI-фича анализирует историю бронирований пользователя и рекомендует:
- **Услугу** - какую услугу выбрать
- **Мастера** - к какому мастеру записаться
- **Время** - в какое время лучше забронировать

---

## 🔍 Алгоритм работы

### Шаг 1: Получение истории бронирований

```javascript
const userBookings = await getUserBookings(userId);
```

Система получает все бронирования пользователя из базы данных.

---

### Шаг 2: Проверка наличия истории

#### **Сценарий А: Нет истории бронирований**

Если пользователь еще ничего не бронировал:

1. **Загружаются популярные услуги:**
   - Берется список активных услуг (первые 10)
   - Рекомендуется первая доступная услуга

2. **Загружаются мастера:**
   - Берется список активных мастеров (первые 5)
   - Сортируются по рейтингу (от большего к меньшему)
   - Рекомендуется мастер с самым высоким рейтингом

3. **Рекомендуемое время:**
   - По умолчанию: `10:00` (утреннее время)

**Результат:**
```json
{
  "service": { "id": 1, "title": "Мужская стрижка", ... },
  "master": { "id": 1, "name": "Александр Иванов", "rating": 4.5, ... },
  "time": "10:00",
  "reason": "Нет истории бронирований. Рекомендуем популярные услуги."
}
```

---

#### **Сценарий Б: Есть история бронирований**

Если у пользователя есть завершенные бронирования:

### Шаг 3: Анализ истории

1. **Фильтрация завершенных бронирований:**
```javascript
const completedBookings = userBookings.filter(b => b.status === 'completed');
```
   - Берем только завершенные бронирования (статус `completed`)
   - Они показывают реальные предпочтения пользователя

2. **Подсчет частоты услуг:**
```javascript
const serviceCounts = {};
completedBookings.forEach(booking => {
  serviceCounts[booking.service_id] = (serviceCounts[booking.service_id] || 0) + 1;
});
```
   - Считаем, сколько раз пользователь бронировал каждую услугу
   - Например: "Мужская стрижка" - 5 раз, "Йога" - 2 раза

3. **Подсчет частоты мастеров:**
```javascript
const masterCounts = {};
completedBookings.forEach(booking => {
  if (booking.master_id) {
    masterCounts[booking.master_id] = (masterCounts[booking.master_id] || 0) + 1;
  }
});
```
   - Считаем, сколько раз пользователь выбирал каждого мастера
   - Например: "Александр Иванов" - 3 раза, "Мария Петрова" - 2 раза

4. **Подсчет частоты времени:**
```javascript
const timeCounts = {};
completedBookings.forEach(booking => {
  const hour = booking.time.split(':')[0]; // Берем только час (14 из "14:30")
  timeCounts[hour] = (timeCounts[hour] || 0) + 1;
});
```
   - Считаем, в какое время пользователь чаще всего бронирует
   - Например: 14:00 - 4 раза, 10:00 - 2 раза

---

### Шаг 4: Выбор рекомендаций

1. **Рекомендуемая услуга:**
```javascript
const recommendedServiceId = Object.keys(serviceCounts).sort(
  (a, b) => serviceCounts[b] - serviceCounts[a]
)[0];
```
   - Находим услугу, которую пользователь бронировал чаще всего
   - Сортируем по убыванию частоты и берем первую

2. **Рекомендуемый мастер:**
```javascript
const recommendedMasterId = Object.keys(masterCounts).sort(
  (a, b) => masterCounts[b] - masterCounts[a]
)[0];
```
   - Находим мастера, которого пользователь выбирал чаще всего
   - Сортируем по убыванию частоты и берем первую

3. **Рекомендуемое время:**
```javascript
const recommendedHour = Object.keys(timeCounts).sort(
  (a, b) => timeCounts[b] - timeCounts[a]
)[0] || '10';
const recommendedTime = `${recommendedHour.padStart(2, '0')}:00`;
```
   - Находим час, в который пользователь чаще всего бронировал
   - Если нет истории времени - используем 10:00 (утро)

---

### Шаг 5: Получение деталей

```javascript
const [recommendedService, recommendedMaster] = await Promise.all([
  recommendedServiceId ? getServiceById(Number(recommendedServiceId)) : null,
  recommendedMasterId ? getMasterById(Number(recommendedMasterId)) : null,
]);
```

Загружаем полную информацию об услуге и мастере из базы данных.

---

### Шаг 6: Формирование ответа

```javascript
return {
  service: recommendedService,      // Полная информация об услуге
  master: recommendedMaster,        // Полная информация о мастере
  time: recommendedTime,            // Рекомендуемое время (например, "14:00")
  reason: 'Рекомендации основаны на вашей истории бронирований.',
  stats: {
    totalBookings: userBookings.length,
    completedBookings: completedBookings.length,
    favoriteService: recommendedService?.title,
    favoriteMaster: recommendedMaster?.name,
  },
};
```

---

## 📊 Пример работы

### Пример 1: Новый пользователь

**Входные данные:**
- Нет истории бронирований

**Процесс:**
1. Загружаются популярные услуги → "Мужская стрижка классическая"
2. Загружаются мастера → "Александр Иванов" (рейтинг 4.5)
3. Время по умолчанию → "10:00"

**Результат:**
```json
{
  "service": { "title": "Мужская стрижка классическая", "price": 1290 },
  "master": { "name": "Александр Иванов", "rating": 4.5 },
  "time": "10:00",
  "reason": "Нет истории бронирований. Рекомендуем популярные услуги."
}
```

---

### Пример 2: Пользователь с историей

**Входные данные:**
- 5 завершенных бронирований:
  - "Мужская стрижка" (3 раза) с мастером "Александр Иванов" (2 раза)
  - "Йога" (2 раза) с мастером "Анна Козлова" (2 раза)
  - Время: 14:00 (3 раза), 10:00 (2 раза)

**Процесс:**
1. Анализ услуг: "Мужская стрижка" - 3 раза (самая частая)
2. Анализ мастеров: "Александр Иванов" - 2 раза (самый частый)
3. Анализ времени: 14:00 - 3 раза (самое частое)

**Результат:**
```json
{
  "service": { "title": "Мужская стрижка классическая", "price": 1290 },
  "master": { "name": "Александр Иванов", "rating": 4.5 },
  "time": "14:00",
  "reason": "Рекомендации основаны на вашей истории бронирований.",
  "stats": {
    "totalBookings": 5,
    "completedBookings": 5,
    "favoriteService": "Мужская стрижка классическая",
    "favoriteMaster": "Александр Иванов"
  }
}
```

---

## 🎯 Где используется

### Backend:
- **Endpoint:** `GET /api/ai/recommendations`
- **Требует:** Авторизацию (JWT токен)
- **Возвращает:** JSON с рекомендациями

### Frontend:
- **Страница:** Главная (`/`)
- **Условие:** Только для авторизованных пользователей
- **Отображение:** Красивый блок с рекомендациями

---

## 💡 Преимущества алгоритма

1. **Простота и эффективность:**
   - Не требует сложных ML моделей
   - Быстро работает
   - Легко понять и модифицировать

2. **Персонализация:**
   - Учитывает реальные предпочтения пользователя
   - Чем больше история - тем точнее рекомендации

3. **Fallback для новых пользователей:**
   - Если нет истории - рекомендует популярное
   - Не оставляет пользователя без рекомендаций

4. **Расширяемость:**
   - Легко добавить новые факторы (сезонность, день недели)
   - Можно добавить машинное обучение в будущем

---

## 🔮 Возможные улучшения

1. **Учет дополнительных факторов:**
   - День недели (понедельник vs выходные)
   - Сезонность
   - Цена (бюджет пользователя)

2. **Коллаборативная фильтрация:**
   - "Пользователи, похожие на вас, также выбирали..."
   - Анализ поведения других пользователей

3. **Машинное обучение:**
   - Обучение модели на исторических данных
   - Более точные предсказания

4. **A/B тестирование:**
   - Тестирование разных алгоритмов
   - Выбор наиболее эффективного

---

## 📝 Итог

AI-фича работает как **умный помощник**, который:
- Анализирует твою историю бронирований
- Находит твои предпочтения
- Рекомендует то, что тебе скорее всего понравится
- Помогает быстрее выбрать услугу, мастера и время

**Это не настоящий AI с нейросетями**, а **умный алгоритм анализа данных**, который делает персональные рекомендации на основе твоей истории.

---

## 🧪 Как протестировать

1. Зарегистрируйся и войди в систему
2. Создай несколько бронирований
3. Заверши их (админ должен изменить статус на `completed`)
4. Обнови главную страницу
5. Увидишь AI рекомендации на основе твоей истории!

