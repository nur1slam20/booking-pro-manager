# 🔗 Подключение проекта к GitHub

## Шаг 1: Создать репозиторий на GitHub

1. Перейдите на [github.com](https://github.com)
2. Нажмите кнопку **"+"** в правом верхнем углу → **"New repository"**
3. Заполните форму:
   - **Repository name**: `booking-pro-manager` (или любое другое имя)
   - **Description**: `Backend API для системы онлайн-бронирования услуг`
   - **Visibility**: Public или Private (на ваше усмотрение)
   - **НЕ** отмечайте "Initialize this repository with a README" (у нас уже есть код)
   - **НЕ** добавляйте .gitignore или license (у нас уже есть)
4. Нажмите **"Create repository"**

## Шаг 2: Скопировать URL репозитория

После создания репозитория GitHub покажет вам страницу с инструкциями. **Скопируйте URL** репозитория, он будет выглядеть так:
- HTTPS: `https://github.com/ваш-username/booking-pro-manager.git`
- SSH: `git@github.com:ваш-username/booking-pro-manager.git`

## Шаг 3: Подключить remote и запушить код

После того как скопируете URL, выполните эти команды в терминале:

```bash
cd "/Users/nurislam/Desktop/Booking Pro Manager"

# Добавить remote (замените URL на свой)
git remote add origin https://github.com/ваш-username/booking-pro-manager.git

# Переименовать ветку в main (если нужно)
git branch -M main

# Запушить код
git push -u origin main
```

## Альтернатива: Использовать GitHub CLI

Если у вас установлен GitHub CLI (`gh`), можно создать репозиторий прямо из терминала:

```bash
cd "/Users/nurislam/Desktop/Booking Pro Manager"

# Создать репозиторий на GitHub (будет запрошена авторизация)
gh repo create booking-pro-manager --public --source=. --remote=origin --push
```

---

**После успешного push ваш код будет на GitHub! 🎉**


