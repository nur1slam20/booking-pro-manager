# 🔧 Исправление проблемы с Git репозиторием

## Проблема:
Git репозиторий был инициализирован в неправильной директории (`/Users/nurislam` вместо папки проекта).

## Решение: Создать правильный репозиторий

### Вариант 1: Перенести репозиторий в папку проекта (Рекомендуется)

1. **Сохраните текущий remote** (URL GitHub репозитория):
   ```bash
   cd "/Users/nurislam/Desktop/Booking Pro Manager"
   git remote get-url origin
   ```
   Запомните URL (например: `https://github.com/nur1slam20/booking-pro-manager.git`)

2. **Создайте новый репозиторий в правильной папке**:
   ```bash
   cd "/Users/nurislam/Desktop/Booking Pro Manager"
   rm -rf .git  # Удаляем старый .git (если есть в этой папке)
   git init
   git branch -M main
   git remote add origin https://github.com/nur1slam20/booking-pro-manager.git
   git add .
   git commit -m "Initial commit - fixed repository structure"
   git push -u origin main --force
   ```

### Вариант 2: Использовать Root Directory в Render

Если не хотите менять структуру Git, можно указать правильный Root Directory в Render:

1. В настройках Web Service на Render
2. В поле **"Root Directory"** укажите: `Desktop/Booking Pro Manager`
3. Сохраните и перезапустите деплой

**НО**: Это не рекомендуется, так как структура репозитория будет неправильной.

---

## Рекомендуемое решение:

Используйте **Вариант 1** - это правильный способ, который исправит структуру репозитория.

