# ✅ Чеклист деплоя Backend на Render

Используйте этот чеклист, чтобы ничего не забыть!

## 📋 Подготовка

- [ ] Аккаунт на Render.com создан
- [ ] GitHub репозиторий готов: https://github.com/nur1slam20/booking-pro-manager

---

## 🔷 Шаг 1: Создание PostgreSQL базы данных

### На Render.com:

1. [ ] Зашел на https://render.com
2. [ ] Нажал **"New +"** → **"PostgreSQL"**
3. [ ] Заполнил форму:
   - [ ] Name: `booking-pro-db`
   - [ ] Database: `booking_db`
   - [ ] User: `booking_user`
   - [ ] Region: `Frankfurt` (или ближайший)
   - [ ] PostgreSQL Version: `16`
   - [ ] Plan: `Free`
4. [ ] Нажал **"Create Database"**
5. [ ] Скопировал **Internal Database URL** (похож на: `postgresql://...@dpg-xxx/booking_db`)

**Internal Database URL**: _________________________________

---

## 🔷 Шаг 2: Генерация JWT_SECRET

Выполни в терминале на своем компьютере:

```bash
openssl rand -hex 32
```

**JWT_SECRET**: _________________________________

---

## 🔷 Шаг 3: Создание Web Service

### На Render.com:

1. [ ] Нажал **"New +"** → **"Web Service"**
2. [ ] Подключил репозиторий: `nur1slam20/booking-pro-manager`
3. [ ] Заполнил настройки:

   **Basic Settings:**
   - [ ] Name: `booking-pro-api`
   - [ ] Environment: `Node`
   - [ ] Region: тот же, что у базы (Frankfurt)
   - [ ] Branch: `main`
   - [ ] Root Directory: оставил пустым (`.`)

   **Build & Deploy:**
   - [ ] Build Command: `npm install`
   - [ ] Start Command: `npm start`

   **Environment Variables** (добавил все):
   - [ ] `NODE_ENV` = `production`
   - [ ] `PORT` = `10000`
   - [ ] `DATABASE_URL` = `<Internal Database URL из шага 1>`
   - [ ] `JWT_SECRET` = `<сгенерированная строка из шага 2>`
   - [ ] `JWT_EXPIRES_IN` = `7d`
   - [ ] `FRONTEND_URL` = `http://localhost:5173` (пока временно)

4. [ ] Нажал **"Create Web Service"**
5. [ ] Дождался первого деплоя (может занять 2-5 минут)

**URL вашего API**: https://________________.onrender.com

---

## 🔷 Шаг 4: Выполнение миграций

### В Render Dashboard:

1. [ ] Зашел в свой Web Service (`booking-pro-api`)
2. [ ] Открыл вкладку **"Shell"**
3. [ ] Выполнил команду:
```bash
psql $DATABASE_URL -f migrations/001_create_tables.sql
```

**Результат**: должно быть сообщение `CREATE TABLE` для каждой таблицы

---

## 🔷 Шаг 5: Создание тестового админа

В том же Shell (Render Dashboard → Shell):

1. [ ] Выполнил команду:
```bash
ADMIN_EMAIL=admin@example.com ADMIN_PASSWORD=admin123456 npm run create-admin
```

**Результат**: должно быть сообщение "✅ Администратор успешно создан!"

---

## 🔷 Шаг 6: Проверка работы API

### В терминале на своем компьютере:

1. [ ] Проверил health check:
```bash
curl https://ваш-url.onrender.com/health
```

Должно вернуться: `{"message":"API работает","timestamp":"..."}`

2. [ ] Проверил API info:
```bash
curl https://ваш-url.onrender.com/
```

Должно вернуться информация об API с endpoints

3. [ ] Проверил регистрацию:
```bash
curl -X POST https://ваш-url.onrender.com/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"name":"Test User","email":"test@test.com","password":"test123456"}'
```

Должно вернуться объект с user и token

---

## ✅ Готово!

После выполнения всех шагов ваш Backend API будет доступен по адресу:
**https://ваш-url.onrender.com**

---

## 📝 Тестовые данные для проверки:

**Admin аккаунт:**
- Email: `admin@example.com`
- Password: `admin123456`

**Тестовый пользователь:**
- Email: `test@test.com`
- Password: `test123456`

---

## 🐛 Если что-то не работает:

1. Проверь логи в Render Dashboard → Logs
2. Убедись, что все переменные окружения установлены
3. Проверь, что база данных доступна (проверь Internal Database URL)
4. Посмотри на статус деплоя (должен быть "Live")

---

**Удачи с деплоем! 🚀**

