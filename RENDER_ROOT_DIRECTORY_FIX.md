# 🔧 КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ: Root Directory в Render

## ⚠️ ПРОБЛЕМА:
Render ищет `package.json` в `/opt/render/project/src/`, но он находится в корне репозитория.

## ✅ РЕШЕНИЕ (ВАРИАНТ 1 - Рекомендуемый):

### Шаг 1: Удалить текущий сервис и создать заново
1. В Dashboard Render найдите ваш Web Service `booking-pro-api`
2. Нажмите на три точки (⋮) рядом с названием
3. Выберите **"Delete"** и подтвердите удаление

### Шаг 2: Создать Web Service заново
1. Нажмите **"New +"** → **"Web Service"**
2. Подключите репозиторий: `nur1slam20/booking-pro-manager`
3. В форме создания **НЕ ЗАПОЛНЯЙТЕ** поле "Root Directory" - оставьте его **ПОЛНОСТЬЮ ПУСТЫМ**
4. Заполните остальные настройки:
   - **Name**: `booking-pro-api`
   - **Environment**: `Node`
   - **Build Command**: `npm install`
   - **Start Command**: `npm start`
5. Добавьте переменные окружения
6. Нажмите **"Create Web Service"**

---

## ✅ РЕШЕНИЕ (ВАРИАНТ 2 - Изменить существующий):

### Шаг 1: Открыть настройки
1. В Dashboard Render найдите ваш Web Service `booking-pro-api`
2. Нажмите на название сервиса
3. Перейдите во вкладку **"Settings"** (самая правая вкладка вверху)

### Шаг 2: Найти и исправить Root Directory
1. Прокрутите вниз до раздела **"Build & Deploy"**
2. Найдите поле **"Root Directory"**
3. **ВАЖНО**: 
   - Если там написано `src` - **УДАЛИТЕ это значение**
   - Если там что-то написано - **УДАЛИТЕ всё**
   - Поле должно быть **ПОЛНОСТЬЮ ПУСТЫМ** (пустая строка)
4. Нажмите **"Save Changes"** (вверху или внизу страницы)

### Шаг 3: Принудительно перезапустить деплой
После сохранения:
1. Перейдите во вкладку **"Manual Deploy"** (слева в меню)
2. Нажмите **"Clear build cache & deploy"**
3. Подтвердите

---

## 🔍 Как проверить, что настройка правильная:

После сохранения, в логах деплоя вы должны увидеть:
```
==> Running build command 'npm install'...
```

И НЕ должно быть пути `/opt/render/project/src/package.json`

Должно быть просто поиск в корне проекта.

---

## ❓ Если ничего не помогает:

Попробуйте явно указать Root Directory как `.` (точка):
1. В поле "Root Directory" введите: `.` (одна точка)
2. Сохраните
3. Перезапустите деплой

---

## 📸 Где найти поле Root Directory:

На странице Settings:
```
Settings
├── General
│   └── Name, Region, Branch...
├── Build & Deploy          ← ИЩИТЕ ЗДЕСЬ
│   ├── Build Command
│   ├── Start Command
│   └── Root Directory      ← ВОТ ЭТО ПОЛЕ!
└── ...
```

