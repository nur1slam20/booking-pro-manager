# 💰 Выполнение миграций на бесплатном Render (без Shell)

## ✅ Метод 1: Локальное выполнение через External Database URL (РЕКОМЕНДУЕТСЯ)

### Шаг 1: Получить External Database URL

1. В Render Dashboard откройте вашу PostgreSQL базу данных `booking-pro-db`
2. Найдите раздел **"Connections"**
3. Найдите **"External Connection String"**
4. Скопируйте URL (он будет выглядеть как: `postgresql://user:password@host:port/database`)

### Шаг 2: Выполнить миграции локально

**Вариант А: Если у вас установлен PostgreSQL (psql)**

```bash
cd "/Users/nurislam/Desktop/Booking Pro Manager"

# Выполнить миграции (замените YOUR_EXTERNAL_URL на скопированный URL)
psql "YOUR_EXTERNAL_URL" -f migrations/001_create_tables.sql
```

**Вариант Б: Если psql не установлен**

1. Установите PostgreSQL: https://www.postgresql.org/download/macosx/
2. Или используйте Docker (если установлен):
   ```bash
   docker run -it --rm postgres psql "YOUR_EXTERNAL_URL" -f - < migrations/001_create_tables.sql
   ```

### Шаг 3: Создать админа через API

После того как backend задеплоится и миграции выполнены:

1. Сначала зарегистрируйте обычного пользователя:
```bash
curl -X POST https://your-backend-url.onrender.com/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Admin User",
    "email": "admin@example.com",
    "password": "admin123456"
  }'
```

2. Затем обновите роль на `admin` через SQL (локально):
```bash
# Подключитесь к базе и обновите роль
psql "YOUR_EXTERNAL_URL" -c "UPDATE users SET role = 'admin' WHERE email = 'admin@example.com';"
```

---

## ✅ Метод 2: Использовать онлайн SQL редактор

Можно использовать бесплатные онлайн инструменты:

### Вариант А: DBeaver Cloud (бесплатно)
1. Зарегистрируйтесь на https://dbeaver.io/cloud/
2. Добавьте подключение к вашей базе (используя External Database URL)
3. Выполните SQL из файла `migrations/001_create_tables.sql`

### Вариант Б: pgAdmin (установить локально)
1. Скачайте pgAdmin: https://www.pgadmin.org/download/
2. Добавьте подключение используя External Database URL
3. Выполните SQL из файла миграций

### Вариант В: Онлайн SQL редакторы
- https://www.elephantsql.com/ (имеет веб-консоль)
- Или другие онлайн PostgreSQL инструменты

---

## ✅ Метод 3: Скрипт для выполнения через Node.js (локально)

Создайте временный скрипт для выполнения миграций:

```bash
cd "/Users/nurislam/Desktop/Booking Pro Manager"

# Установите переменную окружения с External Database URL
export DATABASE_URL="YOUR_EXTERNAL_URL_HERE"

# Запустите скрипт (создадим его ниже)
node scripts/run-migrations.js
```

Но для этого нужен скрипт. Давайте создадим его.

---

## 🎯 Самый простой способ (РЕКОМЕНДУЕТСЯ):

### Вариант 1: Если у вас macOS с Homebrew:

```bash
# Установить PostgreSQL (если нет)
brew install postgresql@16

# Или использовать существующий psql
cd "/Users/nurislam/Desktop/Booking Pro Manager"

# Получите External Database URL из Render Dashboard
# Затем выполните:
psql "YOUR_EXTERNAL_URL" -f migrations/001_create_tables.sql
```

### Вариант 2: Использовать Docker (если установлен):

```bash
cd "/Users/nurislam/Desktop/Booking Pro Manager"

# Получите External Database URL из Render Dashboard
docker run -i --rm postgres:16 psql "YOUR_EXTERNAL_URL" < migrations/001_create_tables.sql
```

---

## 📝 Быстрая инструкция:

1. **Получите External Database URL** из Render (PostgreSQL → Connections → External Connection String)
2. **Выполните миграции локально** (любым из методов выше)
3. **Проверьте** что таблицы созданы
4. **Создайте админа через API**, затем обновите роль через SQL

---

## 🔧 Создание скрипта для миграций через Node.js

Если хотите использовать Node.js скрипт, я могу создать его.

