# 🔧 Исправление ошибки деплоя на Render

## Проблема:
Render ищет `package.json` в неправильной директории (`/opt/render/project/src/`), но он находится в корне проекта.

## Решение:

В настройках Web Service на Render нужно изменить **Root Directory**:

### Шаг 1: Откройте настройки сервиса
1. В Dashboard Render найдите ваш Web Service `booking-pro-api`
2. Нажмите на его название
3. Перейдите во вкладку **"Settings"** (вверху)

### Шаг 2: Измените Root Directory
1. Найдите раздел **"Build & Deploy"**
2. Найдите поле **"Root Directory"**
3. Убедитесь, что оно **ПУСТОЕ** или содержит **`.`** (точка)
4. Если там написано `src` - **УДАЛИТЕ это значение, оставьте пустым**
5. Нажмите **"Save Changes"**

### Шаг 3: Перезапустите деплой
После сохранения Render автоматически начнёт новый деплой с правильными настройками.

---

## Альтернативное решение (если первое не помогло):

Если Root Directory уже пустой, попробуйте явно указать:
- **Root Directory**: `.` (просто точка)

---

## Структура проекта должна быть:
```
booking-pro-manager/          <- корень (здесь должен быть package.json)
├── package.json              <- должен быть здесь!
├── src/
│   ├── server.js
│   ├── app.js
│   └── ...
├── migrations/
├── frontend/
└── ...
```

**Render должен видеть package.json в корне репозитория, а не в src/**

